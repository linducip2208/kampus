@extends('portal.layout', ['heading' => 'Tagihan & pembayaran'])
@section('content')
<x-tabler.alert type="info" title="Ledger pembayaran">Setiap pembayaran terverifikasi dialokasikan ke invoice; status tidak diubah tanpa transaksi.</x-tabler.alert>
<div class="row row-cards">@forelse($enrollment->invoices as $invoice)@php($outstanding = number_format(intdiv(App\Support\Money::toMinorUnits($invoice->outstanding_amount), 100), 0, ',', '.'))<div class="col-12 col-lg-6"><div class="card"><div class="card-body"><div class="d-flex align-items-start justify-content-between gap-3"><div><div class="font-monospace fw-bold">{{ $invoice->invoice_number }}</div><div class="text-secondary small mt-1">Jatuh tempo {{ $invoice->due_on?->translatedFormat('d F Y') ?? 'belum ditentukan' }}</div></div><x-tabler.status :value="$invoice->status" /></div><div class="row mt-4"><div class="col"><div class="text-secondary small">Nilai invoice</div><div class="h3 mb-0">Rp {{ number_format(intdiv(App\Support\Money::toMinorUnits($invoice->total_amount), 100), 0, ',', '.') }}</div></div><div class="col text-end"><div class="text-secondary small">Sisa</div><div class="h3 mb-0 text-orange">Rp {{ $outstanding }}</div></div></div></div></div></div>@empty<div class="col-12"><x-tabler.empty-state title="Tidak ada tagihan" description="Invoice baru akan tampil setelah diterbitkan oleh bagian keuangan." icon="ti-file-invoice" /></div>@endforelse</div>
@endsection