@extends('portal.layout', ['heading' => 'Pembayaran'])
@section('content')<div class="rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-display text-2xl font-bold">Tagihan & pembayaran</h2><p class="mt-2 text-sm text-slate-500">Setiap pembayaran dialokasikan ke invoice sebagai ledger transaksi.</p><div class="mt-6 space-y-3">@foreach($enrollment->invoices as $invoice)<div class="flex flex-col gap-4 rounded-2xl border border-slate-200 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p class="font-mono text-sm font-bold text-slate-700">{{ $invoice->invoice_number }}</p><p class="mt-1 text-sm text-slate-500">Jatuh tempo {{ $invoice->due_on?->translatedFormat('d F Y') }}</p></div><div class="sm:text-right"><p class="font-display text-lg font-bold">Rp {{ number_format($invoice->total_amount,0,',','.') }}</p><p class="mt-1 text-xs text-amber-600">Sisa Rp {{ number_format($invoice->outstanding_amount,0,',','.') }} · {{ ucfirst($invoice->status) }}</p></div></div>@endforeach</div></div>@endsection
