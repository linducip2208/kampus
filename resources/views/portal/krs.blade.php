@extends('portal.layout', ['heading' => 'KRS & hasil studi'])
@section('content')<div class="rounded-2xl border border-slate-200 bg-white p-6"><div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p class="text-sm text-slate-500">{{ $enrollment->studyPlans->first()?->semester?->name }}</p><h2 class="font-display text-2xl font-bold">Kartu Rencana Studi</h2></div><span class="rounded-lg bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">{{ ucfirst($enrollment->studyPlans->first()?->status ?? 'draft') }}</span></div><div class="mt-6 overflow-x-auto"><table class="w-full min-w-[600px] text-left text-sm"><thead class="border-b border-slate-200 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-3 py-3">Mata kuliah</th><th class="px-3 py-3">Kelas</th><th class="px-3 py-3">SKS</th><th class="px-3 py-3">Nilai</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($enrollment->studyPlans->first()?->items ?? [] as $item)<tr><td class="px-3 py-4"><p class="font-semibold">{{ $item->classSection->offering->course->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $item->classSection->offering->course->code }}</p></td><td class="px-3 py-4">{{ $item->classSection->code }}</td><td class="px-3 py-4">{{ $item->credits }}</td><td class="px-3 py-4 font-bold">{{ $item->grade?->gradeScale?->grade ?? '—' }}</td></tr>@endforeach</tbody></table></div></div>@endsection
