@extends('portal.layout', ['heading' => 'Ringkasan akademik'])
@section('content')
<div class="reveal rounded-3xl bg-[#0b1220] p-6 text-white md:p-8"><p class="text-sm text-cyan-300">Selamat datang kembali, {{ explode(' ', $enrollment->studentProfile->full_name)[0] }}.</p><div class="mt-6 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><h2 class="font-display text-3xl font-bold">Semester {{ $enrollment->cohort == now()->year ? 'awal' : 'berjalan' }}</h2><p class="mt-2 max-w-lg text-slate-300">{{ $enrollment->studyProgram->name }} · Angkatan {{ $enrollment->cohort }}</p></div><div class="rounded-2xl bg-white/10 px-5 py-4"><p class="text-xs text-slate-400">Status akademik</p><p class="mt-1 font-bold text-emerald-300">{{ ucfirst($enrollment->status) }}</p></div></div></div>
<div class="mt-6 grid gap-4 sm:grid-cols-3">@foreach([['IPK',''.number_format($gpa,2), 'Dari nilai terbit'],['SKS semester',$plan?->total_credits ?? 0,'KRS disetujui'],['Tagihan berjalan','Rp '.number_format($enrollment->invoices->sum(fn($invoice) => $invoice->total_amount - $invoice->paid_amount),0,',','.'),'Sisa pembayaran']] as $stat)<div class="card-lift rounded-2xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">{{ $stat[0] }}</p><p class="mt-3 font-display text-2xl font-bold">{{ $stat[1] }}</p><p class="mt-2 text-xs text-slate-400">{{ $stat[2] }}</p></div>@endforeach</div>
<div class="mt-8 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><section class="rounded-2xl border border-slate-200 bg-white p-6"><div class="flex items-center justify-between"><h3 class="font-display text-lg font-bold">Kelas semester ini</h3><a href="{{ route('portal.krs') }}" class="text-sm font-bold text-blue-600">Lihat KRS</a></div><div class="mt-5 divide-y divide-slate-100">@forelse($plan?->items ?? [] as $item)<div class="flex items-center justify-between gap-4 py-4"><div><p class="font-semibold">{{ $item->classSection->offering->course->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $item->classSection->offering->course->code }} · Kelas {{ $item->classSection->code }} · {{ $item->credits }} SKS</p></div><span class="rounded-lg bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">Nilai tersedia</span></div>@empty<p class="py-6 text-sm text-slate-500">Belum ada KRS.</p>@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-6"><h3 class="font-display text-lg font-bold">Dosen wali</h3><div class="mt-5 rounded-xl bg-blue-50 p-4"><p class="font-semibold text-blue-900">{{ $enrollment->advisor?->employee?->full_name ?? 'Belum ditentukan' }}</p><p class="mt-1 text-sm text-blue-700">{{ $enrollment->advisor?->academic_rank ?? 'Dosen wali' }}</p></div><p class="mt-5 text-sm leading-6 text-slate-500">Ajukan konsultasi untuk rencana studi, prasyarat mata kuliah, atau strategi kelulusan.</p></section></div>
@endsection
