@extends('layouts.storefront', ['title' => 'Blog Campus ERP — Wawasan tata kelola kampus'])
@section('content')<section class="bg-[#0b1220] py-16 text-white"><div class="mx-auto max-w-7xl px-5 lg:px-8"><p class="text-sm font-bold text-cyan-300">INSIGHT KAMPUS</p><h1 class="mt-3 font-display text-4xl font-bold">Catatan untuk kampus yang ingin bergerak dengan data.</h1><p class="mt-4 max-w-2xl text-slate-300">Praktik akademik, tata kelola data, dan operasional universitas dalam bahasa yang bisa dipakai tim sehari-hari.</p></div></section><section class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div class="mb-8 flex gap-3 overflow-x-auto"><a class="whitespace-nowrap rounded-full bg-[#0b1220] px-4 py-2 text-sm font-semibold text-white" href="{{ route('blog') }}">Semua artikel</a>@foreach($categories as $category)<a class="whitespace-nowrap rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600 hover:border-blue-500" href="{{ route('blog.category', $category->slug) }}">{{ $category->name }}</a>@endforeach</div><div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">@forelse($posts as $post)<article class="card-lift overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="h-32 bg-gradient-to-br from-blue-700 to-cyan-400 p-5 text-white"><span class="rounded-lg bg-white/15 px-3 py-1 text-xs font-semibold">{{ $post->category?->name ?? 'Campus ERP' }}</span></div><div class="p-6"><p class="text-xs text-slate-400">{{ $post->published_at?->translatedFormat('d F Y') }}</p><h2 class="mt-2 font-display text-xl font-bold">{{ $post->title }}</h2><p class="mt-3 text-sm leading-6 text-slate-600">{{ $post->excerpt }}</p><a class="mt-5 inline-block text-sm font-bold text-blue-600" href="{{ route('blog.show', $post->slug) }}">Baca artikel →</a></div></article>@empty<p class="text-slate-500">Belum ada artikel terbit.</p>@endforelse</div><div class="mt-8">{{ $posts->links() }}</div></section>@endsection
