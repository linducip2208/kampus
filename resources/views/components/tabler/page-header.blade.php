@props(['title', 'subtitle' => null, 'breadcrumbs' => []])
<div {{ $attributes->class(['page-header', 'd-print-none']) }}><div class="container-xl">@if($breadcrumbs)<x-tabler.breadcrumb :items="$breadcrumbs" />@endif<div class="row g-2 align-items-center"><div class="col"><h2 class="page-title">{{ $title }}</h2>@if($subtitle)<div class="text-secondary mt-1">{{ $subtitle }}</div>@endif</div>@isset($actions)<div class="col-auto ms-auto d-print-none"><div class="btn-list">{{ $actions }}</div></div>@endisset</div></div></div>
