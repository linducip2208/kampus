<div class="modal-body"><div class="row g-3">
    <div class="col-12"><label class="form-label required" for="section-{{ $schedule?->id ?? 'new' }}">Kelas</label>@if($schedule)<input type="hidden" name="class_section_id" value="{{ $schedule->class_section_id }}"><select class="form-select" id="section-{{ $schedule->id }}" disabled><option>{{ $schedule->classSection?->offering?->course?->code }} · {{ $schedule->classSection?->offering?->course?->name }} — {{ $schedule->classSection?->code }}</option></select><div class="form-hint">Kelas tidak dapat dipindahkan saat mengubah slot.</div>@else<select class="form-select" id="section-new" name="class_section_id" required>@foreach($sections as $section)<option value="{{ $section->id }}" @selected(old('class_section_id')===$section->id)>{{ $section->offering?->course?->code }} · {{ $section->offering?->course?->name }} — {{ $section->code }}</option>@endforeach</select>@endif</div>
    <div class="col-12 col-sm-6"><label class="form-label required">Hari</label><select class="form-select" name="day_of_week" required>@foreach([1=>'Senin',2=>'Selasa',3=>'Rabu',4=>'Kamis',5=>'Jumat',6=>'Sabtu',7=>'Minggu'] as $value=>$label)<option value="{{ $value }}" @selected((int)old('day_of_week', $schedule?->day_of_week ?? 1)===$value)>{{ $label }}</option>@endforeach</select></div>
    <div class="col-12 col-sm-6"><label class="form-label">Ruangan</label><input class="form-control" name="room" value="{{ old('room', $schedule?->room) }}" maxlength="100" placeholder="Kosongkan untuk kelas online"></div>
    <div class="col-12 col-sm-6"><label class="form-label required">Mulai</label><input type="time" class="form-control" name="starts_at" value="{{ old('starts_at', $schedule?->starts_at?->format('H:i')) }}" required></div>
    <div class="col-12 col-sm-6"><label class="form-label required">Selesai</label><input type="time" class="form-control" name="ends_at" value="{{ old('ends_at', $schedule?->ends_at?->format('H:i')) }}" required></div>
</div></div>
