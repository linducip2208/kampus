@extends('lecturer.layout', ['heading' => 'Kelas saya'])
@section('content')
<p class="text-secondary mb-4">Lihat konteks kelas dan jumlah mahasiswa pada setiap mata kuliah yang Anda ampu.</p>
<div class="row row-cards">@forelse($sections as $section)<div class="col-12 col-md-6 col-xl-4"><div class="card h-100"><div class="card-body"><div class="d-flex justify-content-between"><span class="badge bg-secondary-lt">{{ $section->code }}</span><x-tabler.status :value="$section->offering->status" /></div><h3 class="mt-4 mb-1">{{ $section->offering->course->name }}</h3><div class="text-secondary">{{ $section->offering->course->code }} · {{ $section->offering->course->credits }} SKS</div><div class="row g-3 mt-3"><div class="col-6"><div class="bg-body-tertiary rounded p-3"><div class="text-secondary small">Mahasiswa</div><div class="h2 mb-0">{{ $section->studyPlanItems->count() }}</div></div></div><div class="col-6"><div class="bg-body-tertiary rounded p-3"><div class="text-secondary small">Pertemuan</div><div class="h2 mb-0">{{ $section->meetings->count() }}</div></div></div></div></div><div class="card-footer text-secondary small"><i class="ti ti-map-pin me-1"></i>{{ $section->room ?: 'Ruang belum ditentukan' }} · {{ $section->schedules->count() }} slot</div></div></div>@empty<div class="col-12"><x-tabler.empty-state title="Belum ada kelas" description="Penugasan akademik akan tampil setelah disimpan." icon="ti-door-off" /></div>@endforelse</div>
@endsection